---
label: Categores
hide_body: true
fields:
- type: list
  name: categories
  label: Categories
  config:
    use_select: false
  description: Categories are a collection of items in the same folder/ directory.
    More generic then tag/ filters e.g. Cat, Dogs, Fish, Birds
